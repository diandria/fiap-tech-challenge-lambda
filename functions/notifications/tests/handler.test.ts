import { handler } from '../src/handler';

describe('Notifications function skeleton', () => {
  it('should resolve GIVEN no implementation yet WHEN the handler runs', async () => {
    await expect(handler()).resolves.toBeUndefined();
  });
});
