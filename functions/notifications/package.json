{
  "name": "notifications-function",
  "version": "1.0.0",
  "private": true,
  "description": "Entrega notificacoes a partir de eventos do SNS",
  "scripts": {
    "build": "esbuild src/handler.ts --bundle --platform=node --target=node22 --outfile=dist/index.js",
    "test": "jest",
    "test:coverage": "jest --coverage",
    "lint": "eslint src tests"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/aws-lambda": "^8.10.145",
    "@types/jest": "^29.5.12",
    "@types/node": "^22.10.0",
    "esbuild": "^0.24.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=22"
  }
}
