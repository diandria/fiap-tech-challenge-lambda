/**
 * Emite o JWT de cliente a partir do CPF.
 *
 * Esqueleto. O contrato e a validacao entram na tarefa seguinte; 501 deixa
 * explicito que a rota existe e ainda nao faz nada.
 */
export async function handler(): Promise<{ statusCode: number }> {
  return { statusCode: 501 };
}
